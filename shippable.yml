language: java

jdk:
   - oraclejdk7

services:
  - mysql

build:
  pre_ci_boot:
    image_name: drydock/u14javall
    image_tag: prod
  ci:
   - mvn clean cobertura:cobertura
   - mvn test
 
integrations:
  hub:
    - integrationName: GCR
      type: gcr

notifications:
  email: false

